# The wallet itself is the Rust core, loaded through ctypes from the standard
# library — there is nothing to install for it. Build it with
# `make -C rustcli build`, or point CAUSEWAYBAY_LIB at a copy.
